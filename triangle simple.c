#include<stdio.h>
#include<math.h>
void main()
{
    float h,b;
    float area;
    printf(\n "enter the value of the heightand base of the triangle");
    scanf("%f%f",&h,&b);
    area of the triangle=(0.5*h*b);
    ptintf(\%f area of the triangle);
}
