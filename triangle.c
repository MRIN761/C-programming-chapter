#include<stdio.h>
#include<math.h>
int main()
{
    float b,h, area;
    printf("enter base and hight of the triangle:");
    scanf("%f%f", &b, &h);
    area=(b*h)/2.0;
    printf(" area of the triangle=%f");
}
