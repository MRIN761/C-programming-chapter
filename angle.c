#include<stdio.h>
#include<math.h>
 main()
{
  int angle;
  float y;
  angle =0;
  printf("angle   cos(angle)\n\n");
  {
    while (angle<=180);
    y=cos(angle);
  }
    printf("the value of y is %f\n",y);
}
